---
import { getEntryOrThrow } from "@utils/content";
import PageLayout from "../layouts/PageLayout.astro";

const articles = await getEntryOrThrow("pages", "articles");
---

<PageLayout page={articles} />
