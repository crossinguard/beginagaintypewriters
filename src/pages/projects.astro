---
import { getEntryOrThrow } from "@utils/content";
import PageLayout from "../layouts/PageLayout.astro";

const projects = await getEntryOrThrow("pages", "projects");
---

<PageLayout page={projects} />
