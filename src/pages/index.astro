---
import { getEntryOrThrow } from "@utils/content";
import PageLayout from "../layouts/PageLayout.astro";

const home = await getEntryOrThrow("pages", "home");
---

<PageLayout page={home} displayTitle={false} />
