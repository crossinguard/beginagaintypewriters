---
import { getEntryOrThrow } from "@utils/content";
import PageLayout from "../layouts/PageLayout.astro";

const design = await getEntryOrThrow("pages", "design");
---

<PageLayout page={design} />
