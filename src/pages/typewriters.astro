---
import { getEntryOrThrow } from "@utils/content";
import PageLayout from "../layouts/PageLayout.astro";

const typewriters = await getEntryOrThrow("pages", "typewriters");
---

<PageLayout page={typewriters} />
