---
import { getEntryOrThrow } from "@utils/content";
import PageLayout from "../layouts/PageLayout.astro";

const contact = await getEntryOrThrow("pages", "contact");
---

<PageLayout page={contact} />
